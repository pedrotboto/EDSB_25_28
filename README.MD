# EDSB_25_28_Bank_Telemarketing_Campaigns

Short description of your project — what it does and why it exists.

## Features
- Feature 1
- Feature 2
- Feature 3

## Requirements
See `requirements.txt` for dependencies.

## Installation
```bash
git clone https://github.com/your-username/your-repo.git
cd your-repo
pip install -r requirements.txt

## Intruções trabalho
1-download git for windows
2-install git extension on vscode https://code.visualstudio.com/docs/sourcecontrol/github
3-Criar conta github
4-Na extensão fazer clone repository e por o url
5-abrir o terminal no vscode e adicionar isto, editando o nome e o e-mail (do github)
git config --global user.name "YOUR_NAME"
git config --global user.email "YOUR_EMAIL"
6-no terminal correr pip install -r requirements.txt
7-Se forem necessárias API KEY pedir .env